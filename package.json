{
  "name": "json-server-buensabor",
  "version": "0.0.1",
  "description": "JsonServer de mock para el dashboard del buen sabor",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "copy-db": "node copy-db.js",
    "json-server":"json-server --watch temp-db.json --port 3000",
    "serve": "npm-run-all copy-db json-server"
  },
  "author": "Gonzalo Vega - Franco Albornoz - Ignacio Ramet",
  "license": "ISC",
  "devDependencies": {
    "npm-run-all": "^4.1.5"
  },
  "dependencies": {
    "json-server": "^1.0.0-alpha.23"
  }
}
